# 嘉立创EDA 电流表原理图分步设计指南

## 前置条件
- 已登录嘉立创EDA标准版
- 已创建工程"电流表设计_MSPM0"
- 当前在Sheet_1原理图页面

---

## 步骤1：放置MSPM0G3507 MCU（核心元件）

### 操作方法：
1. 在左侧"元件库"面板的搜索框中输入：`MSPM0G3507`
2. 选择"立创商城"搜索引擎
3. 在搜索结果中选择 **MSPM0G3507SRHB**（VQFN-32封装）
   - 或者选择 **MSPM0G3507SPMR**（LQFP-64封装，更大更方便手工焊接）
4. **双击**元件或点击"放置"按钮
5. 在画布上点击一个位置放置MCU

### MSPM0G3507 引脚功能说明（VQFN-32版本）：
```
Pin 1: VDDA - 模拟电源 (3.3V)
Pin 2: PA0/AIN0 - ADC通道0（电流采样输入）
Pin 3: PA1/AIN1 - ADC通道1
Pin 4: PA2/DAC - DAC输出（可选）
Pin 5: PA3 - 通用IO
Pin 6: PA4 - 通用IO  
Pin 7: PA5/SCK - SPI时钟
Pin 8: PA6/MISO - SPI主入
Pin 9: PA7/MOSI - SPI主出
Pin 10: PB0/CS - SPI片选
Pin 11: PB1/CE - SPI模式选择
Pin 12: PB2 - 通用IO（模块检测）
Pin 13: PB3 - 通用IO（按键）
Pin 14: PF0 - 地址位0
Pin 15: PF1 - 地址位1
Pin 16: PF2 - 地址位2
Pin 17: PF3 - 地址位3
Pin 18: VSSA - 模拟地
Pin 19: VSS - 数字地
Pin 20: VDD - 数字电源 (3.3V)
Pin 21-32: 更多GPIO和电源引脚...
```

---

## 步骤2：放置电源部分元件

### 2.1 整流桥
1. 搜索：`1N4007W`
2. 选择1N4007肖特基二极管（4个组成整流桥）
3. 放置在MCU右侧

### 2.2 DC-DC升压（MT3608）
1. 搜索：`MT3608`
2. 如果找不到，搜索：`DC-DC` 或 `升压`
3. 放置在整流桥后方

### 2.3 LDO稳压器（可选，用于3.3V）
1. 搜索：`AMS1117` 或 `7805`
2. 选择低压差稳压器
3. 放置在DC-DC后方

### 2.4 储能电容
1. 搜索：`C_Tan_SMD_A_US`
2. 选择100μF/25V钽电容
3. 放置在电源输出端

### 2.5 去耦电容
1. 搜索：`C_0603_US`
2. 选择0.1μF (104)电容 × 6个
3. 选择1μF (105)电容 × 2个
4. 放置在MCU电源引脚附近

---

## 步骤3：放置电流测量电路元件

### 3.1 电流采样电阻
1. 搜索：`R_0603_US`
2. 选择1Ω, 1%电阻
3. 放置在测量绕组回路中

### 3.2 运算放大器
1. 搜索：`LM358` 或 `LM358N`
2. 选择双运算放大器
3. 放置在采样电阻旁边

### 3.3 运放反馈电阻
1. 搜索：`R_0603_US`
2. 选择100kΩ电阻 × 2个
3. 选择10kΩ电阻 × 2个
4. 按反馈回路布局

### 3.4 滤波电容
1. 搜索：`C_0603_US`
2. 选择0.1μF电容 × 2个
3. 放置在ADC输入端

---

## 步骤4：放置接口元件

### 4.1 无线模块接口（8Pin排母）
1. 搜索：`HDR-F-2.54_1x8`
2. 选择8Pin 单列排母
3. 放置在PCB右侧边缘

### 4.2 地址拨码开关
1. 搜索：`DIP` 或 `拨码开关`
2. 选择4位拨码开关
3. 放置在PCB左上侧

### 4.3 功能按键
1. 搜索：`K2-3.6×6.1_SMD`
2. 选择轻触按键
3. 放置在PCB前面板

### 4.4 香蕉插座
1. 搜索：`香蕉` 或 `Banana`
2. 选择4mm香蕉插座（红黑各3个）
3. 放置在PCB边缘

---

## 步骤5：放置显示元件

### 5.1 数码管显示
1. 搜索：`SEG-TH_0.56×1_Cathode`
2. 选择0.56吋 四位一体数码管（共阴）
3. 或搜索：`TM1637` 显示驱动芯片
4. 放置在PCB上方

### 5.2 LED指示灯
1. 搜索：`LED-0603_R`（红色）× 3个
2. 搜索：`LED-0603_G`（绿色）× 1个
3. 放置在状态指示区

### 5.3 LED限流电阻
1. 搜索：`R_0603_US`
2. 选择1kΩ电阻 × 4个

---

## 步骤6：连线（按模块分组）

### 6.1 电源部分连线
```
无线取电回路：
  次级绕组N2 → 整流桥输入 → 滤波C(100μF) → MT3608输入
  MT3608输出(3.3V) → 系统VCC总线
  GND → 系统GND总线

去耦：
  MCU VCC引脚 → 0.1μF → GND (每个VCC引脚)
  MCU VCC引脚 → 1μF → GND

放电回路：
  3.3V → 放电电阻(100Ω/1W) → 放电按键 → GND
```

### 6.2 电流测量连线
```
采样电阻R_sense(1Ω)：
  一端 → GND
  另一端 → 运放U2(LM358)同相输入(+)

运放连接：
  同相输入(+) ← R_sense非接地端
  反相输入(-) → 10kΩ → GND
  反相输入(-) → 100kΩ → 输出
  输出 → MSPM0 PA0 (AIN0)
  
  电源+ → 3.3V
  电源- → GND

滤波：
  PA0 → 0.1μF → GND
  PA0 → 1μF → GND
```

### 6.3 SPI无线接口连线
```
8Pin排母(J1) ← MSPM0：
  Pin1 (3.3V) ← VCC
  Pin2 (GND) ← GND
  Pin3 (SCK) ← PA5/SCK
  Pin4 (MISO) ← PA6/MISO
  Pin5 (MOSI) ← PA7/MOSI
  Pin6 (CSN) ← PB0/CS
  Pin7 (CE) ← PB1/CE
  Pin8 (DETECT) ← PB2
```

### 6.4 地址拨码开关连线
```
4位拨码开关(SW2) ← MSPM0：
  Bit0 → PF0 (内部上拉)
  Bit1 → PF1 (内部上拉)
  Bit2 → PF2 (内部上拉)
  Bit3 → PF3 (内部上拉)
  公共端 → GND
```

### 6.5 数码管/TM1637连线
```
TM1637 ← MSPM0：
  CLK ← PA6 (或其他GPIO)
  DIO ← PA5 (或其他GPIO)
  VCC → 3.3V
  GND → GND
```

### 6.6 按键和LED连线
```
功能按键(SW3)：
  一端 → PB3 (内部上拉)
  另一端 → GND

LED指示灯：
  LED1(电源) → 1kΩ → 3.3V (常亮)
  LED2(状态) → 1kΩ → GPIO
  LED3(报警) → 1kΩ → GPIO
```

---

## 步骤7：布局优化

### 布局分区原则：
1. **高压区**（输入插座、保险丝）：PCB左上角，物理隔离
2. **无线取电区**（磁芯、整流桥、DC-DC）：PCB左侧中部
3. **电流测量区**（采样电阻、运放）：PCB中部
4. **核心控制区**（MCU）：PCB中心
5. **显示区**（数码管、LED）：PCB上方
6. **接口区**（排母、香蕉座）：PCB右侧/下侧

### 关键布局规则：
- MCU芯片丝印朝上，不得被遮挡
- 磁芯放置在边缘，初级绕组可见
- 放电按钮外露于面板
- 高压区域与低压区域物理隔离
- 无线模块接口边缘放置，方便插拔

---

## 步骤8：检查与保存

1. **电气规则检查（ERC）**：
   - 点击"设计"→"ERC检查"
   - 修正所有错误和警告

2. **原理图检查清单**：
   - [ ] 所有元件已放置
   - [ ] 电源连接正确（3.3V、GND）
   - [ ] SPI接口连接正确
   - [ ] 测量电路连接正确
   - [ ] 无悬空引脚

3. **保存工程**：
   - Ctrl+S 保存
   - 建议定期保存

4. **生成BOM清单**：
   - 点击"设计"→"物料清单(BOM)"
   - 核对元件清单

---

## 快捷操作提示

| 操作 | 快捷键 |
|------|--------|
| 放置元件 | 双击元件 |
| 旋转元件 | R |
| 布线 | W 或点击布线工具 |
| 删除 | Delete |
| 撤销 | Ctrl+Z |
| 保存 | Ctrl+S |
| 放大 | Scroll Up |
| 缩小 | Scroll Down |
| 平移 | 鼠标中键拖动 |

---

## 常见问题解答

**Q1: 找不到MSPM0G3507怎么办？**
- 确认选择的是"立创商城"搜索引擎
- 尝试搜索完整型号：MSPM0G3507SRHB
- 如仍找不到，可联系嘉立创客服申请添加元件库

**Q2: 元件库没有的元件怎么办？**
- 使用"设计"→"符号向导"创建自定义符号
- 或选择功能等效的元件替代

**Q3: 如何修改元件参数？**
- 双击元件打开属性对话框
- 在值(Value)栏输入参数值（如10kΩ）

**Q4: 如何连接两个引脚？**
- 点击"布线"工具
- 从一个引脚拖动到另一个引脚
- 松开鼠标完成连接

---

## 下一步

完成原理图设计后：
1. 点击"设计"→"原理图转PCB"
2. 在PCB页进行布局和布线
3. 生成Gerber文件用于打样

---

**文档版本：** v1.0  
**创建日期：** 2026-07-29  
**适用项目：** 自制电流表及其读表系统竞赛设计
