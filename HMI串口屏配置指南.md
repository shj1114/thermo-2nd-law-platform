# 正点原子 HMI 串口屏配置指南

## 一、硬件连接

### 1.1 接线方式

正点原子HMI串口屏使用4根线与MSPM0G3507连接：

```
HMI屏幕        MSPM0G3507
-------        ----------
VCC    <--->   3.3V (注意电压匹配)
GND    <--->   GND
TX     <--->   P1.2 (UCA0RXD)  // HMI的TX连MCU的RX
RX     <--->   P1.3 (UCA0TXD)  // HMI的RX连MCU的TX
```

### 1.2 注意事项

- **电压匹配**：HMI屏幕通常支持3.3V或5V TTL电平，MSPM0G3507使用3.3V
- **交叉接线**：HMI的TX接MCU的RX，HMI的RX接MCU的TX
- **共地**：确保两个设备有公共地线
- **波特率**：默认9600，可通过USART HMI软件修改

---

## 二、HMI屏幕软件配置

### 2.1 下载USART HMI软件

访问官方网站下载最新版：
- 淘晶驰官方：http://www.tjc1688.com
- 指令集 Wiki：http://wiki.tjc1688.com/commands/index.html

### 2.2 新建工程

1. 打开USART HMI软件
2. 新建工程，选择对应屏幕型号（如HMI01-0430）
3. 设置工程参数：
   ```
   bauds=9600        // 波特率设置
   bkcmd=3           // 返回数据格式(0-3)
   dims=100          // 字符串变量最大长度
   ```

### 2.3 页面设计

#### 常用控件类型：

| 控件类型 | 说明 | 变量名示例 |
|---------|------|-----------|
| 文本控件 | 显示文字 | t0, t1, t2... |
| 数值控件 | 显示数字（整型） | n0, n1, n2... |
| 浮点控件 | 显示浮点数 | f0, f1, f2... |
| 字符串控件 | 显示字符串 | s0, s1, s2... |
| 按钮控件 | 触发事件 | b0, b1, b2... |
| 曲线控件 | 显示曲线 | c0, c1, c2... |
| 图片控件 | 显示图片 | p0, p1, p2... |
| 进度条 | 显示进度 | j0, j1, j2... |

#### 示例界面 - 电流监测仪：

**页面0：主界面**
- t0: 标题 "电流监测仪"
- n0: 实时电流值（单位：0.01A）
- t1: 电流单位 "A"
- n1: 最大电流记录
- n2: 最小电流记录
- t2: "电流趋势图"
- c0: 电流曲线控件

**页面1：设置界面**
- s0: 设备地址
- b0: 校准按钮
- b1: 清零按钮

### 2.4 事件脚本

#### 按钮事件示例：

```
// 按钮 b0 按下事件 - 发送校准命令
b0.val=1
prints "CAL"
printh 0d 0a

// 按钮 b1 按下事件 - 清零记录
b1.val=1
prints "CLR"
printh 0d 0a

// 页面跳转
page 1    // 跳转到页面1
page 0    // 返回页面0
```

#### 数据发送配置：

在变量控件的属性中设置：
- 勾选"**数据变化自动上传**" - 变量值变化时自动发送
- 或使用**定时发送**功能

---

## 三、通信协议

### 3.1 指令格式

#### MCU → HMI（发送指令到屏幕）：

```
指令格式：命令 参数1,参数2,... \xFF\xFF\xFF
结束标志：\xFF\xFF\xFF  (三个0xFF字节)
```

#### 常用指令列表：

| 指令 | 格式 | 说明 | 示例 |
|------|------|------|------|
| page | `page 页面号` | 页面跳转 | `page 0` |
| prints | `prints 变量名,值` | 设置变量值 | `prints n0,123` |
| printb | `printb 变量名,值` | 设置双字节变量 | `printb n0,65535` |
| printl | `printl 变量名,值` | 设置长整型变量 | `printl n0,100000` |
| prstr | `prstr 字符串控件,值` | 设置字符串 | `prstr s0,Hello` |
| vis | `vis 控件名,0/1` | 显示/隐藏控件 | `vis t0,0` |
| tsw | `tsw 控件名,0/1` | 触摸使能 | `tsw b0,1` |
| add | `add 曲线控件,值` | 添加曲线数据点 | `add c0,50` |
| cle | `cle 曲线控件` | 清除曲线数据 | `cle c0` |
| click | `click 按钮控件` | 触发按钮事件 | `click b0` |
| rest | `rest` | 复位屏幕 | `rest` |
| ref | `ref 控件名` | 重绘控件 | `ref n0` |

#### HMI → MCU（屏幕返回数据）：

格式1 - 文本格式（bkcmd=3）：
```
变量名.值\r\n
示例：n0.123\r\n
     s0.Hello World\r\n
```

格式2 - 二进制格式（bkcmd=0/1/2）：
```
根据bkcmd值不同返回不同格式
```

### 3.2 通信流程图

```
┌─────────────┐                    ┌─────────────┐
│  MSPM0G3507  │                    │   HMI屏幕    │
└──────┬──────┘                    └──────┬──────┘
       │                                  │
       │──── page 0 ───────────────────→│  跳转页面0
       │                                  │
       │                                  │──── 发送当前变量(自动)
       │←── n0.0\r\n ───────────────────│  初始电流=0
       │                                  │
       │──── prints n0,150 ─────────────→│  更新电流为1.50A
       │                                  │
       │                                  │──── 显示更新
       │                                  │
       │──── add c0,150 ────────────────→│  曲线添加数据点
       │                                  │
       │                                  │
       │        ...循环通信...             │
       │                                  │
       │←── b0.1\r\n ───────────────────│  按钮b0被按下
       │                                  │
       │──── prints n0,0 ───────────────→│  清零电流值
       │──── cle c0 ────────────────────→│  清除曲线
       │                                  │
```

---

## 四、MSPM0G3507 引脚配置

### 4.1 UART0 引脚映射

| 功能 | 引脚 | 说明 |
|------|------|------|
| UCA0TXD | P1.3 | MCU串口发送 → HMI接收 |
| UCA0RXD | P1.2 | MCU串口接收 ← HMI发送 |

### 4.2 GPIO 初始化

```c
// P1.3 = UCA0TXD
P1->SEL0 |= BIT3;
P1->SEL1 &= ~BIT3;

// P1.2 = UCA0RXD  
P1->SEL0 |= BIT2;
P1->SEL1 &= ~BIT2;
```

### 4.3 串口参数

```
波特率：9600 (可改为115200)
数据位：8
停止位：1
校验位：无
```

---

## 五、调试建议

### 5.1 串口调试工具

使用USB转TTL模块连接HMI屏幕到电脑，使用串口助手调试：
- 推荐工具：SSCOM、XCOM、正点原子串口助手
- 设置相同波特率和数据格式

### 5.2 常见问题

| 问题 | 可能原因 | 解决方案 |
|------|---------|---------|
| 屏幕无响应 | 波特率不匹配 | 检查两端波特率设置 |
| 乱码 | 电压不匹配 | 确认电平为3.3V TTL |
| 指令无效 | 缺少结束标志 | 添加 \xFF\xFF\xFF |
| 不返回数据 | bkcmd设置 | 设置bkcmd=3 |
| 按钮无反应 | 触摸未使能 | 检查tsw设置 |

### 5.3 快速验证步骤

1. 连接HMI屏幕到电脑，用串口助手发送：
   ```
   page 0\xFF\xFF\xFF
   ```
   如果屏幕跳转到页面0，说明通信正常

2. 发送：
   ```
   prints n0,123\xFF\xFF\xFF
   ```
   如果数值控件n0显示123，说明数据显示正常

3. 在HMI软件中设置按钮发送数据，用串口助手接收验证

---

## 六、界面设计建议

### 6.1 电流监测仪界面布局

```
┌─────────────────────────────┐
│     电流监测仪 V1.0         │ ← 标题
├─────────────────────────────┤
│                             │
│   实时电流:  1.25 A         │ ← 数值显示
│                             │
│   最大: 2.00 A              │
│   最小: 0.00 A              │
│                             │
├─────────────────────────────┤
│  ┌─────────────────────┐  │
│  │   电流趋势曲线       │  │ ← 曲线控件
│  └─────────────────────┘  │
│                             │
├─────────────────────────────┤
│  [校准] [清零] [设置]       │ ← 按钮
└─────────────────────────────┘
```

### 6.2 颜色代码表

| 颜色 | 代码 | 颜色 | 代码 |
|------|------|------|------|
| 黑色 | 0000 | 白色 | FFFF |
| 红色 | F800 | 绿色 | 07E0 |
| 蓝色 | 001F | 黄色 | FFE0 |
| 橙色 | FD20 | 紫色 | F81F |

---

## 七、参考资源

- 官方资料中心：http://wiki.tjc1688.com
- 指令集手册：http://wiki.tjc1688.com/commands/index.html
- 正点原子论坛：http://www.openedv.com/forum.php
- MSPM0G3507数据手册：TI官网
